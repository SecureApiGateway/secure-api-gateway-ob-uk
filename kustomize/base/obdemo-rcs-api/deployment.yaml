apiVersion: apps/v1
kind: Deployment
metadata:
  name: obdemo-rcs-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: obdemo-rcs-api
  template:
    metadata:
      labels:
        app: obdemo-rcs-api
    spec:
      containers:
      - image: obdemo-rcs-api
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: platform-config
        livenessProbe:
          httpGet:
            path: /
            port: http
        name: obdemo-rcs-api
        resources:
          requests:
            memory: "500Mi"
            cpu: "10m"
          limits:
            memory: "500Mi"
        ports:
        - containerPort: 8083
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /
            port: http
